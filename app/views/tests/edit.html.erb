<div class="container">
  <div class="row">
    <div class="col-12 col-md-6 col-lg-4">
      <%= simple_form_for @test do |f| %>
        <p>Semana <%= @test.id %> </p>
        <p>Este teste está <%= @test.private ? "aberto " : "fechado" %></p>
        <%= f.input :question1, label: false %>
        <%= f.input :question2, label: false %>
        <%= f.input :question3, label: false %>
        <%= f.input :question4, label: false %>
        <%= f.input :private, label: "Aberto" %>
        <%= f.submit "Editar", class:"btn btn-primary" %>
       <% end %>
      <%= render 'shared/footer' %>
    </div>
  </div>
</div>
