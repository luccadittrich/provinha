<div class="container mt-2">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6 col-lg-4">
      <div class="devise-page">
        <h2>Criar conta</h2>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>
          <div class="form-inputs">
            <%= f.input :first_name,
                label: "Nome",
                autofocus: true,
                input_html: { autocomplete: "Nome completo" } %>
            <%= f.input :last_name,
                label: "Sobrenome",
                autofocus: true,
                input_html: { autocomplete: "Nome completo" } %>
            <div class="d-flex justify-content-around">
              <%= f.input :teacher,
               label: "Professor",
               autofocus: false,
               input_html: { autocomplete: "teacher?" } %>
              <%= f.input :president,
               label: "Presidente da classe",
               autofocus: false,
               input_html: { autocomplete: "Presidente da classe?" } %>
            </div>
            <%= f.input :photo,
                as: :file,
                label: "Foto" %>
            <%= f.input :email,
                required: true,
                input_html: { autocomplete: "email" }%>
            <%= f.input :password,
                label: "Senha",
                required: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
            <%= f.input :password_confirmation,
                label: "Confirme sua senha",
                required: true,
                input_html: { autocomplete: "new-password" } %>
            <div class="form-actions d-flex mt-1">
              <%= f.button :submit, "Criar conta", class: "w-100 m-1 p-1 btn btn-success", style: "color: #fff" %>
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
